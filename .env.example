# CloudCode Server Configuration
# Copy this file to .env and customize your settings

# Basic Authentication
PASSWORD=cloudcode123
USER=coder
SUDO_PASSWORD=cloudcode123

# Workspace Configuration
WORKSPACE=/workspace

# Git Integration (Optional)
# Set these to auto-clone your repositories
GIT_REPO=                    # Format: username/repository-name
GIT_TOKEN=                   # GitHub Personal Access Token for private repos

# SSH Key for Git (Optional)
# Paste your private SSH key (base64 encoded) for Git operations
SSH_PRIVATE_KEY=

# Custom VS Code Extensions (Optional)
# Comma-separated list of extension IDs
EXTENSIONS=ms-python.python,ms-vscode.vscode-typescript-next,eamodio.gitlens

# SSL Configuration (Optional - for production)
DOMAIN=localhost            # Your domain name for SSL
ACME_EMAIL=admin@example.com # Email for Let's Encrypt certificates

# Network Configuration
PORT=8080                   # Port for code-server (change if needed)

# Cloud Deployment Secrets (Choose your platform)

# === RENDER.COM ===
# Set these in Render dashboard under Environment Variables
# RENDER_SERVICE_NAME=cloudcode-server
# PASSWORD=your-secure-password

# === RAILWAY.APP ===
# Set these in Railway dashboard under Variables
# RAILWAY_VOLUME_MOUNT_PATH=/workspace
# PASSWORD=your-secure-password

# === REPLIT ===
# Set these in Replit Secrets tab
# PASSWORD=your-secure-password
# REPLIT_USER=your-username

# Additional Security (Production)
# Enable these for production deployments
# HTTPS_ONLY=true
# SECURITY_HEADERS=true
# RATE_LIMITING=true